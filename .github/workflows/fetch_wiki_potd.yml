name: 📸 Fetch Wikimedia Picture of the Day

# 定义工作流触发条件
on:
  # 1. 允许手动运行
  workflow_dispatch:
  # 2. 当指定的脚本或工作流文件变动时自动运行
  push:
    paths:
      - 'fetch_wiki_image.py'
      - '.github/workflows/fetch_wiki_potd.yml'

# 设置环境变量
env:
  TZ: Asia/Shanghai # 设置工作流中的时间基准为上海时区 (Asia/Shanghai)

jobs:
  fetch_and_commit:
    runs-on: ubuntu-latest
    
    steps:
      # 1. 检出代码
      - name: ⬇️ Checkout repository (获取完整历史用于推送)
        uses: actions/checkout@v4
        with:
          # fetch-depth: 0 对于需要 git push 的工作流是必要的
          fetch-depth: 0 

      # 2. 设置 Python 环境
      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      # 3. 安装依赖 (requests 和 pytz)
      - name: ⚙️ Install dependencies
        run: pip install requests pytz

      # 4. 运行 Python 脚本
      - name: 🚀 Run script to fetch and download data
        # 脚本将创建 YYYY/MM/YYYY-MM-DD-HH-MM-SS.ext (图片) 和 YYYY/MM/YYYY-MM-DD-HH-MM-SS_meta.txt (元数据)
        run: python fetch_wiki_image.py

      # 5. Git 配置 (必须，用于标识提交者身份)
      - name: 👤 Configure Git User
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "github-actions-bot@users.noreply.github.com"
          
      # 6. 检查是否有文件变动并准备提交 (关键修改：确保 git add 在检查之前运行)
      - name: 📝 Stage changes and Check for changes
        id: stage
        run: |
          # 尝试添加所有新创建的目录和文件到暂存区
          git add .
          
          # 检查暂存区是否为空。如果为空，则没有变化。
          if git diff --cached --quiet; then
            echo "::set-output name=has_changes::false"
          else
            echo "::set-output name=has_changes::true"
          fi

      # 7. 提交并推送新文件 (仅当有变动时)
      - name: 📤 Commit and push new file
        # 仅当步骤 6 检测到文件变动时才执行
        if: steps.stage.outputs.has_changes == 'true'
        run: |
          # 创建提交信息，包含日期
          COMMIT_MESSAGE="feat: Add POTD for $(TZ='Asia/Shanghai' date +'%Y-%m-%d')"
          
          # 提交（文件已经在上一步被 git add . 了）
          git commit -m "$COMMIT_MESSAGE"
          
          # 推送回仓库
          git push
